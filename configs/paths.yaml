# =============================================================================
# Path configuration
# Resolved by src/paths.py against the repo root.
# =============================================================================
raw_data_dir: Data

# Outputs.
figures_dir: reports/figures
tables_dir:  reports/tables
decks_dir:   reports/decks